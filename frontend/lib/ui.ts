// Clases compartidas para el estilo de tarjeta de Nocturne — superficie +
// elevación por sombra, sin borde (ver UiDesign/prototype/_ds/nocturne-.../
// readme.md, ".card" + ".elev-sm/md/lg"). Centralizado aquí para que el
// par rounded-md/shadow-* no se retipee (y potencialmente diverja) en cada
// pantalla — igual que `profile.ts` centraliza el mapeo de rol → color.
export const CARD_SM = "rounded-md bg-surface-raised shadow-sm";
export const CARD_MD = "rounded-md bg-surface-raised shadow-md";
